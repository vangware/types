/**
 * Non-primitive type (alias for `object`).
 *
 * @category Common
 * @example
 * ```typescript
 * const anObject: NonPrimitive = {};
 * const anArray: NonPrimitive = [];
 * ```
 */
// eslint-disable-next-line @typescript-eslint/ban-types
export type NonPrimitive = object;
