/**
 * Read-only array.
 *
 * @category Array
 * @example
 * ```typescript
 * const array: ReadOnlyArray<number> = [1, 2, 3];
 * ```
 */
export type ReadOnlyArray<Item = unknown> = ReadonlyArray<Item>;
