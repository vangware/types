pre-push:
    commands:
        clean:
            run: rimraf ./dist
        lint:
            run: eslint {src,tests}/**/*.ts
        test:
            run: tsc --noEmit
        document:
            run: typedoc
